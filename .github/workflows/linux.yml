name: Ubuntu

on:
  push:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  actions: write

jobs:
  build-and-test:
    uses: ./.github/workflows/build.yml
    with:
      os: ubuntu-24.04
      platform: linux-x86_64
      package: false
